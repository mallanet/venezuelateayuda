import { MapExplorer } from "@/components/map-explorer";

export const metadata = {
  title: "Mapa de ayuda — Venezuela Te Ayuda",
};

export default function MapaPage() {
  return <MapExplorer />;
}
