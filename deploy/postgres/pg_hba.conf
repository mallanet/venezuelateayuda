# Internal Docker network only — Postgres is not published on the host.
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             172.16.0.0/12           trust
host    all             all             10.0.0.0/8              trust
host    all             all             all                     scram-sha-256
