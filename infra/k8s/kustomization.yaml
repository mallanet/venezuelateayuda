apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: venezuelateayuda
resources:
  - namespace.yaml
  - postgres.yaml
  - migrate-job.yaml
  - app.yaml
  - ingress.yaml
# secret.example.yaml NO se aplica — crear vta-env a mano.
